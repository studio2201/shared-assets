[package]
name = "shared-core"
version = "3.0.0"
edition = "2024"
description = "Core platform-agnostic types and primitives for UberMetroid shared assets."
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
constant_time_eq = "0.3"
